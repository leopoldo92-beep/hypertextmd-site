# Publish the landing page (2 minutes, one-time)
1. On github.com (logged in as leopoldo92-beep): New repository → name it exactly `hypertextmd-site` → Public → CREATE (no README).
2. Tell Claude "the site repo exists" — it runs `git push -u origin main` from this folder.
3. On the new repo: Settings → Pages → Source: "Deploy from a branch" → Branch: main, folder: / (root) → Save.
Live at: https://leopoldo92-beep.github.io/hypertextmd-site/  (later: point hypertextmd.com here)
